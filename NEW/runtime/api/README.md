# Native API

[`project_facade.cppm`](project_facade.cppm) is the C++23 public facade intended to remain stable when future Python, JavaScript, or Go bindings are added. Those bindings are intentionally out of scope for this migration.
