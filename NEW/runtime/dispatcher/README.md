# Dispatcher

[`command_dispatcher.cppm`](command_dispatcher.cppm) routes command metadata to inline validation or the shared runtime worker pool. Mutation remains owned by the target project session and its projection coordinator.
