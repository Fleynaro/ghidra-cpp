# Runtime Tests

[`runtime_tests.cppm`](runtime_tests.cppm) covers worker execution, event commit/replay, torn-tail recovery, checksum rejection, SQLite-backed projection checkpoints, and trigger coalescing.
