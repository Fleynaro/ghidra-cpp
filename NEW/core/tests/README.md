# Core Tests

[`domain_tests.cppm`](domain_tests.cppm) verifies checked address arithmetic, inclusive range normalization, escaped event fields, and deterministic checksums for the canonical domain/event kernel.
