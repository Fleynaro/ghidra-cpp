# Translation Tests

The shared translation boundary is exercised by [`../../tests/service_tests.cppm`](../../tests/service_tests.cppm), which decodes a compiled SLA through the same core values consumed by the PE/decompiler services.
