add_library(analyzer_external_entry_references STATIC)
target_sources(analyzer_external_entry_references PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES src/external_entry_references.cppm)
target_link_libraries(analyzer_external_entry_references PUBLIC NewGhidra::AnalyzerShared)
target_compile_features(analyzer_external_entry_references PUBLIC cxx_std_23)

if(BUILD_TESTING)
    find_package(GTest CONFIG REQUIRED)
    add_executable(analyzer_external_entry_references_tests)
    target_sources(analyzer_external_entry_references_tests PRIVATE FILE_SET cxx_modules TYPE CXX_MODULES FILES tests/external_entry_references_tests.cppm)
    target_link_libraries(analyzer_external_entry_references_tests PRIVATE analyzer_external_entry_references NewGhidra::AnalyzerTestSupport GTest::gtest_main)
    target_compile_features(analyzer_external_entry_references_tests PRIVATE cxx_std_23)
    add_test(NAME analyzer_external_entry_references_tests COMMAND analyzer_external_entry_references_tests)
endif()
