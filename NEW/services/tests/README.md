# Service Tests

[`service_tests.cppm`](service_tests.cppm) proves the PE and Sleigh service adapters against the real PE and compiled-SLA fixtures. The target also links the decompiler and Function ID service modules to ensure their contract adapters compile in the shared dependency graph.
