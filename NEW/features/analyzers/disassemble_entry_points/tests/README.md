# Disassemble Entry Points Tests

[`disassemble_entry_points_tests.cppm`](disassemble_entry_points_tests.cppm) contains the manager and entry-point pipeline tests. Fixture data is grouped under [`data/`](data/); other analyzer tests live in their corresponding analyzer directory.
