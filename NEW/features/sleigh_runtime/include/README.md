# Public API

[`sleigh_runtime.hpp`](sleigh_runtime.hpp) is the stable public model for decoded instructions. It owns strings, varnodes, operands, flow information, and p-code so callers do not depend on Ghidra's internal classes.

The parent module is documented in [`../README.md`](../README.md).
