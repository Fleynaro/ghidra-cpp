# Boost.Thread Test

[`test.cpp`](test.cpp) uses a real Boost worker thread, mutex, condition variable, and join. The generated consumer is verified against the Boost.Thread FIDB and a non-Boost control.
