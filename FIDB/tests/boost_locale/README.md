# Boost.Locale Test

[`test.cpp`](test.cpp) uses a real locale generator, backend configuration, case conversion, and normalization. Ghidra verifies `generator`, `clear_paths`, and `clear_domains` matches in the independent executable.
