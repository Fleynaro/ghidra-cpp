# Boost.Program_options Test

[`test.cpp`](test.cpp) parses real command-line options into a `variables_map` using `options_description`, `store`, and `notify`. The independent executable is checked with the generated Boost.Program_options FIDB.
