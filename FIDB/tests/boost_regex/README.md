# Boost.Regex Test

[`test.cpp`](test.cpp) performs real regular-expression construction, search, match, and replacement. The pipeline validates implementation functions such as `perl_matcher`, `parse`, and `match_all_states` through original Ghidra Function ID.
