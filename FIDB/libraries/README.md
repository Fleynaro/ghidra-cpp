# Generated FID Libraries

This directory contains packed `.fidb` files emitted by the original Ghidra `FidFileManager` and `FidService` APIs. The zlib database is generated at [`zlib/1.3.1/`](zlib/1.3.1/) by the pipeline in [`../README.md`](../README.md). No database bytes are hand-authored here.
